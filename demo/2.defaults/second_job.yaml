- job:
    name: second-job
    defaults: defaults

    builders:
      - shell: |
          set -xe
          echo "2.SUCCESS!"
          true
- job:
    name: third-job

    builders:
      - shell: |
          set -xe
          echo "3.SUCCESS!"
          true
